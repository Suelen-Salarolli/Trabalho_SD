apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: orders-api-pod-kill
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: orders-api
  duration: 30s
